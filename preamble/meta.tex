%% Set up the meta data here.

% the title displayed on the title page
\newcommand{\doctitle}{\LaTeX\ template \texttt{article-template}}

% the title written in the PDF metadata (pure text, no formatting)
\newcommand{\doctitlestring}{LaTeX template article-template}

% the author
\newcommand{\docauthor}{Yifei Fan}
